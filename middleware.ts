// Middleware disabled to reduce bundle size for Vercel free tier
// Auth is still enforced via login page
// export { auth as middleware } from "@/lib/auth"

// export const config = {
//   matcher: ['/((?!api|_next/static|_next/image|favicon.ico|uploads).*)'],
// }
